/// XML file cue model
///
/// [st] the start time in milliseconds
/// [et] the end time in milliseconds
/// [sub] the sub title
class XmlFileCue {
  final String? st;
  final String? et;
  final String? sub;

  const XmlFileCue({this.st, this.et, this.sub});
}
